<%= form_with(model: hero, local: true) do |form| %>
  <% if hero.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hero.errors.count, "error") %> prohibited this hero from being saved:</h2>

      <ul>
      <% hero.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nickname %>
    <%= form.text_field :nickname, id: :hero_nickname %>
  </div>

  <div class="field">
    <%= form.label :category %>
    <%= form.text_field :category, id: :hero_category %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email, id: :hero_email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
